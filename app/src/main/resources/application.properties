# Default datasource (loaded by Ebean from classpath)
datasource.default=db

# --- Remote (Railway) — used when running from IDE with no MYSQL* env vars ---
datasource.db.username=root
datasource.db.password=haBAJbkkuWAVaBllMcmYZJKWRClSztjB
datasource.db.url=jdbc:mysql://tramway.proxy.rlwy.net:26269/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

# --- Local MySQL (uncomment and comment out Railway block above) ---
# datasource.db.username=root
# datasource.db.password=your_mysql_password
# datasource.db.url=jdbc:mysql://localhost:3306/college_placement?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

ebean.packages=models.sql
ebean.search.packages=models.sql
